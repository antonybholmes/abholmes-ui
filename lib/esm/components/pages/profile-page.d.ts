import { IDBItem, ISessionUser } from "..lib/types";
interface IProps {
    session: ISessionUser;
    positions: IDBItem[];
}
export default function ProfilePage({ session, positions }: IProps): import("react/jsx-runtime").JSX.Element;
export { };

